var image;
(function (image) {
    class View {
        distanceDrawing;
        constructor(distanceDrawing) {
            this.distanceDrawing = distanceDrawing;
            this.distanceDrawing = distanceDrawing;
        }
        generate() {
        }
    }
    image.View = View;
})(image || (image = {}));
